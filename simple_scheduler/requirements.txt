# Core ndscheduler dependencies (Python 2.7.16 compatible pinned versions)
APScheduler == 3.0.0
SQLAlchemy == 1.0.0
future == 0.15.2
tornado == 4.3.0
python-dateutil == 2.2
pytz >= 2015.4

# Python 2.7 backport of concurrent.futures (required by tornado and ndscheduler)
futures == 3.3.0

# Utility
six >= 1.9.0

# Additional dependencies for simple_scheduler only

requests == 2.9.1

# Uncomment this if you want to use Postgres as datastore
#
# psycopg2 == 2.6.1

# Uncomment this if you want to use MySQL as datastore
#
# pymysql == 0.6.7
